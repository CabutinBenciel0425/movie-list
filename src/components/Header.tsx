import Logo from "../ui/Logo";
import Searchbar from "./Searchbar";

function Header() {
  return (
    <header>
      <Logo />
      <Searchbar />
    </header>
  );
}

export default Header;
